<UserControl x:Class="MaterialDesignExtensionsDemo.Controls.ThemesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaterialDesignExtensionsDemo.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500" Background="{DynamicResource MaterialDesignBackground}">
    <Grid>
        <ScrollViewer Padding="16" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto">
            <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Top" MaxWidth="300">
                <DockPanel HorizontalAlignment="Stretch" Height="32">
                    <ToggleButton DockPanel.Dock="Right" IsChecked="{Binding Path=IsDarkTheme}" Margin="16,0,0,0" VerticalAlignment="Center" />
                    <TextBlock FontSize="16" Text="Dark theme enabled" HorizontalAlignment="Left" VerticalAlignment="Center" />
                </DockPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
